import * as vscode from 'vscode';

/**
 * Refreshes the template explorer
 * This is a placeholder - the actual refresh is handled in the template explorer provider
 *
 * @return Promise<void>
 */
export default async function refreshTemplates(): Promise<void> {
	// The logic for refreshing is handled in the tree view provider via the command registration
	// This is just a placeholder for the command
}
